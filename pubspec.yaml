name: docshelper
description: Platform for assignments

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  file_picker: ^1.9.0+1
  firebase_storage: ^3.1.5
  firebase_core: ^0.4.4+3
  firebase_analytics: ^5.0.11
  firebase_auth: ^0.16.0
  cloud_firestore: ^0.13.5
  http: ^0.12.0+4
  flutter_downloader: ^1.4.4
  path_provider: ^1.6.8
  google_sign_in: ^4.4.6
  pull_to_refresh: ^1.5.8
  provider: ^4.0.5+1
  fluttertoast: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - images/